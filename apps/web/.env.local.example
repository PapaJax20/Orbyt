# ─────────────────────────────────────────────────────────────────────────────
# Orbyt — Local Environment Variables
# Copy this file to .env.local and fill in values from `supabase start`
# ─────────────────────────────────────────────────────────────────────────────

# Supabase (run `supabase start` to get these values)
NEXT_PUBLIC_SUPABASE_URL=http://127.0.0.1:54321
NEXT_PUBLIC_SUPABASE_ANON_KEY=sb_publishable_<from supabase start>
SUPABASE_SERVICE_ROLE_KEY=sb_secret_<from supabase start>
DATABASE_URL=postgresql://postgres:postgres@127.0.0.1:54322/postgres

# App
NEXT_PUBLIC_APP_URL=http://localhost:3000

# Email (not required for local dev — invites use clipboard link instead)
# RESEND_API_KEY=re_xxxxxxxxxxxx

# Error tracking (not required for local dev)
# NEXT_PUBLIC_SENTRY_DSN=https://xxx@sentry.io/xxx
# SENTRY_AUTH_TOKEN=sntrys_xxxx
